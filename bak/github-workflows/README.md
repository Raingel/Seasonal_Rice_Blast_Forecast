# Inactive GitHub Actions (archived)

These workflow files were moved out of `.github/workflows/` to disable them.

Reason:
- Keep only the automated latest-data updater active.
- Prevent accidental/manual runs of legacy baseline/audit/rebuild workflows.

Active workflow retained:
- `.github/workflows/seas5_latest_update.yml`
